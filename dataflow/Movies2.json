{
	"name": "Movies2",
	"properties": {
		"sources": [
			{
				"dataset": {
					"referenceName": "movies2",
					"type": "DatasetReference"
				},
				"name": "source1",
				"script": "source(output(movieId as string,title as string,genres as string),allowSchemaDrift: false,validateSchema: false) ~> source1"
			}
		],
		"transformations": [
			{
				"name": "DerivedColumn1",
				"script": "source1 derive(splitgenres = split(genres, '|')) ~> DerivedColumn1"
			}
		]
	}
}